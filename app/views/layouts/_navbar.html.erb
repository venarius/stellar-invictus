<nav class="navbar navbar-expand-lg navbar-dark bg-dark">
    <a class="navbar-brand" href="/"><%= image_tag('logo.png') %></a>
    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarColor02" aria-controls="navbarColor02" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    
    <div class="collapse navbar-collapse" id="navbarColor02">
      <ul class="navbar-nav mr-auto">
        <% if current_user %>
          <%= navbar_item(game_path, 'navbar.game') %>
          <%= navbar_item(game_mails_path, 'navbar.mails') %>
          <%= navbar_item(map_path, 'navbar.map') %>
        <% else %>
          <%= navbar_item(root_path, 'navbar.home') %>
          <%= navbar_item(about_path, 'navbar.about') %>
        <% end %>
      </ul>
      <ul class="navbar-nav">
        <% if current_user %>
          <div class="dropdown">
            <a data-toggle="dropdown"><%= image_tag("avatars/#{current_user.avatar}.jpg", class: 'navbar-avatar') %> <i class="fa fa-caret-down fa-lg"></i></a>
            <div class="dropdown-menu dropdown-menu-right">
              <p class="dropdown-item mb-0"><%= current_user.full_name.truncate(20) %></p>
              <div class="dropdown-divider"></div>
              <%= link_to I18n.t('navbar.profile'), edit_user_registration_path, class: 'dropdown-item' %>
              <%= link_to I18n.t('navbar.logout'), destroy_user_session_path, method: :delete, class: 'dropdown-item', style: 'color: #CC0000' %>
            </div>
          </div>
        <% else %>
          <%= navbar_item(nojs_path, 'navbar.connect') %>
        <% end %>
      </ul>
    </div>
</nav>